У тебя отличная структура проекта: логика аккуратно разделена, зависимости проверяются, логгирование включено, а GUI выделен в отдельный модуль. Это реально продвинутый подход, особенно для прикладного калькулятора. Вот несколько замечаний и рекомендаций:
Новые сообщения
21:26
Дублирование функции initialize_database()
Ты объявляешь initialize_database() дважды. Удали один из вариантов (оставь второй, он правильный):УДАЛИТЬ эту частьdef initialize_database():try:from data.init_db import initial_dbinitial_db()return Trueexcept Exception as e:...

Импорт initial_db() лучше делать вверху
Т.к. ты уже импортируешь его вверху:

from data.init_db import initial_db
...то можно не дублировать этот импорт в теле функции. Так код будет свежее
Логировать ошибки
Сейчас ты показываешь messagebox.showerror, но не логируешь сами ошибки. Добавь logger.exception(...):

except Exception as e:
logger.exception("Ошибка инициализации БД")
root = tk.Tk()
root.withdraw()
messagebox.showerror(
'Ошибка инициализации БД',
f'Не удалось инициализировать базу данных:\n{e}'
)
И так же в main() — в блоке except.